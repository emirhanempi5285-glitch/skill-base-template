name: Template CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  validate-template:
    name: Validate template scaffold
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Validate scaffold
        run: npm run validate

      - name: Validate standard GitHub CLI source layout
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh skill publish --dry-run

      - name: Package smoke test
        run: npm run package -- v0.0.0 # placeholder tag; not a real release version
